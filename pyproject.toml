[project]
name = "semcor-wordnet-integration"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "teanga",
    "wn>=1.1.0",
]

[tool.uv.sources]
teanga = { git = "https://github.com/teangaNLP/teanga" }
